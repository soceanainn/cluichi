*** Settings ***
Documentation    Test script to verify the functionality of boggle.js
Library          SeleniumLibrary
Library          OperatingSystem

*** Variables ***
${BROWSER}       Chrome
${URL}           http://192.168.0.129:2000/boggle  # Replace with the actual URL of the HTML file with the boggle game


*** Test Cases ***
Verify isConnected Function
    Open Browser  ${URL}  ${BROWSER}
    Sleep  2s   # Wait for the page to load

    # Click on the letters to make selections
    Click on Letter  2
    #Sleep  2s   # Wait for the page to load
    Click on Letter  3


    # Verify if the letters have been selected
    Element Should Have Class  css=#row0 > div:nth-child(2)  selected-letter
    Element Should Have Class  css=#row0 > div:nth-child(3)  selected-letter


*** Keywords ***
Click on Letter
    [Arguments]  ${letter_index}
    Click Element  css=#row0 > div:nth-child(${letter_index})

Element Should Have Class
    [Arguments]  ${locator}  ${expected_class}
    ${element}  Get WebElement  ${locator}
    ${actual_class}  Get Element Attribute  ${element}  class
    Should Contain  ${actual_class}  ${expected_class}

